{
    "private": true,
    "scripts": {
        "build:assets": "wp-scripts build --config webpack.assets.config.js",
        "build:blocks": "wp-scripts build --webpack-src-dir=src/resources/blocks/src/ --output-path=src/resources/blocks/build/",
        "build": "npm run build:blocks && npm run build:assets"
    },
    "devDependencies": {
        "@wordpress/i18n": "^5.20.0",
        "@wordpress/scripts": "^30.13.0",
        "nice-forms.css": "^0.1.7",
        "tom-select": "^2.5.2"
    }
}
